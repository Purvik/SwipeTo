name: swipe_to
description: SwipeTo is a wrapper for a chat view widget which can be used initiate callback when user horizontally swipe on it.
version: 0.1.0-nullsafety.1
authors:
  - Purvik Rana <purvik1239@gmail.com>
homepage: https://github.com/Purvik/SwipeTo.git

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: '>=1.10.0-nullsafety.3 <3.0.0'
